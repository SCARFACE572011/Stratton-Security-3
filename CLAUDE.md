@AGENTS.md

# Stratton Security Group — Website Rebuild

## Project Overview
Full redesign and rebuild of strattonsecuritygroup.com as a premium Next.js 16 site.
Original site was on Squarespace. New site is custom Next.js + Tailwind v4 + Framer Motion.

## Tech Stack
- **Framework:** Next.js 16 (App Router, TypeScript)
- **Styling:** Tailwind CSS v4 (CSS-first config via `@theme` in globals.css — no tailwind.config.ts)
- **Animation:** Framer Motion v12 under **LazyMotion strict** (`SiteChrome.tsx`).
  Import `m` (never `motion`) from framer-motion — `motion.*` compiles fine but
  **throws at runtime** inside the strict LazyMotion tree. Only `domAnimation`
  features are loaded (no drag/layout/layoutId/Reorder). Above-the-fold hero
  entrances are CSS-driven (`anim-rise`/`anim-fade`/`anim-word` utilities in
  globals.css) so the headline paints before any JS; keep it that way.
- **Icons:** Lucide React
- **Fonts:** Barlow Semi Condensed (display) + Barlow (headings) + IBM Plex Sans (body)

## Client Info (verified from site audit)
- **Phone:** (424) 440-5554
- **Email:** Info@StrattonSecurityGroup.com
- **Address:** 10940 Wilshire Blvd, Suite 1720, Los Angeles, CA 90024 (single source: `SITE_CONFIG` in constants.ts — CONFIRMED by client 2026-07-23)
- **PPO License:** #122163 (CA Bureau of Security and Investigative Services)
- **Hours:** 24/7/365
- **Tagline:** "Excellence In Protection" / "Always Vigilant"

## Design System
Colors and utilities live in `src/app/globals.css` under `@theme` and `@utility`.
Do NOT create a tailwind.config.ts — Tailwind v4 uses CSS-first configuration.

## Key Files
- `src/lib/constants.ts` — All site content: services, testimonials, stats, config
- `src/app/globals.css` — Full design system (Tailwind v4 @theme + @utility)
- `src/app/layout.tsx` — Root layout with fonts + org schema
- `src/app/schema.tsx` — JSON-LD structured data
- `src/components/layout/Navigation.tsx` — Sticky nav with dropdown + mobile drawer
- `src/components/layout/Footer.tsx` — Full premium footer
- `src/components/home/HeroSection.tsx` — Video hero section
- `src/components/forms/ContactForm.tsx` — Two-step contact form (native submit disabled on purpose — see in-file comment before "simplifying")

## Content TODO (needs client input before launch)
1. **Video:** Homepage video — obtain MP4 from Squarespace CDN or client.
   Update `SITE_CONFIG.heroVideoSrc` in `src/lib/constants.ts`.
2. **Hero poster:** Add `/public/images/hero-poster.jpg` for video fallback.
3. **Leadership:** Add named leadership photos + bios (currently faceless).
4. **LinkedIn URL:** Confirm and add to `SITE_CONFIG.social.linkedin`.
5. **Form endpoint:** Wire `ContactForm.tsx` to email service (Resend, Postmark, etc.)
6. **Client logos:** Identify logos for "Trusted By" section on homepage.
7. **Service area confirmation:** Verify exact coverage counties.

## SEO Notes
- Sitemap at `/sitemap.xml` auto-generated by Next.js
- JSON-LD Organization + LocalBusiness + SecurityService schema on every page
- PPO license #122163 visible in footer on all pages

## Critical URL Fixes Applied (vs. old Squarespace site)
- Old: Careers at `/services` → New: `/careers`
- Old: Contact titled "Private Security Guard" → New: proper `/contact`
- Old: Non-descriptive slugs (`/new-page`, `/construction-1`) → New: semantic slugs
